# 数据置信度与打标规范（wenshuge-data）

> 治理原则（2026-09-09 拍板）：**不确定的数据直接打标，不阻塞入库；
> 打标内容在 source.note 内以固定关键词呈现，后续人工核对以 data PR
> 修订并移除对应标记。**

## 标记关键词（source.note 内查找）

| 关键词 | 含义 | 当前覆盖 |
|---|---|---|
| `机转待抽校` / `待抽校` | 正文为 OpenCC t2s 机器简体，未逐首人工校 | qts 全集（42 片） |
| `kaiyang infer` / `not decidable` | form 为低置信推断（basis 跟在词后）或不可判定置 null | qts 全集 |
| `作者v1 slug 待实体化` | author.id 为拼音 slug（冲突确定性去重），非最终实体键 | qts 全集 |
| `id 重编…M3a` | id 曾经 8 位 base36 重编（历史映射另存 tsv） | tang 选集（历史 note） |
| （无标记） | 人工校勘过的底本正文 | tang/300-poems.jsonl |

## 置信度分层

- **L1 人工校**：tang/300-poems.jsonl —— 底本古诗文网（蘅塘退士通行本），逐行人工清洗入库。
- **L2 机转待核**：qts 全集 —— 简体/体裁/作者 slug 均为 v1 机器产出并打标；2026-09-09 初核 40 首抽检通过
  （`docs/review-sample-qts-2026-09-09.tsv`，固定种子 20260909），不代表全量无错。

## 后续核对流程

1. 发现错字/误判 → 数据仓开 data PR（拾遗式校对）：修一行也是一次贡献；
2. 修订后删除该行 note 中相应标记关键词（如 `待抽校` → 移除），保留底本/转换溯源信息；
3. 定期人工抽校样本可复用 `scripts/convert_quan_tang_shi.py` 的固定种子逻辑扩充。
